var postDR = artifacts.require("postDR");

module.exports = function(deployer, network, accounts) {

    var daily_reward = 0;
    var penalty_rate = 0;
    var dr_contract_address = [];

    //deployer.deploy(postDR, daily_reward, penalty_rate, dr_contract_address);
};